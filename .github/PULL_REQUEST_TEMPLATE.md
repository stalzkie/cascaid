## What does this change?

<!-- One or two sentences: what changed and why. -->

## How was this tested?

<!--
Which tests did you run/add? `uv run pytest tests/unit -q`, integration,
e2e, frontend (`npm test`)? If you couldn't run integration/e2e locally
(e.g. no local Postgres), say so -- CI will still run them.
-->

## Checklist

- [ ] Branched from and targeting `staging` (not `master`)
- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass
- [ ] Relevant test tier(s) pass locally, or I've noted which ones I couldn't run
- [ ] Docs updated if this changes user-facing behavior (README, `docs/`)
